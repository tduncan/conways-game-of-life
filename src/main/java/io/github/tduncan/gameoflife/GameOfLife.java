package io.github.tduncan.gameoflife;

class GameOfLife {
    int[][] nextGeneration(int[][] grid) {
        // process all cells
        // compute when cells die
        // compute when cells continue to live
        return grid;
    }
}

